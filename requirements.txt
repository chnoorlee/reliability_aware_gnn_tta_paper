# PyTorch Geometric migration — pinned environment (Python 3.14).
#
# NOTE: torch here is a CUDA nightly because stable torch wheels for Python 3.14
# were not yet published when this artifact was built.  The experiments are
# full-batch on small graphs and run on CPU; any torch >= 2.4 with a matching
# torch_geometric works.  For a fully stable artifact, recreate the environment
# on Python 3.13 with a stable torch release (e.g. torch==2.6.* + the matching
# torch_geometric) — the code is version-agnostic across torch >= 2.4.
#
# Exact versions used to produce results_torch/:
torch==2.12.0.dev20260408+cu128
torch_geometric==2.8.0
numpy==2.4.4
scipy==1.17.1
